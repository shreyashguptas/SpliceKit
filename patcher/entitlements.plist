<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "https://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Disable sandbox to prevent CloudKit entitlement validation failures -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <!-- Injection entitlements -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.get-task-allow</key>
    <true/>
    <!-- Hardened runtime requires explicit resource entitlements. FCP originally declares -->
    <!-- these; re-signing with an entitlements file replaces the full set, so we must keep -->
    <!-- them here or LiveCam / voiceover record / USB devices stop working. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
